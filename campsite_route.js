// import express from "express";
// import { createCampsite, getAllCampsites, getCampsiteById, updateCampsite, deleteCampsite } from "../controller/campsite_controller.js";
// import { verifyAdmin } from "../middleware/authMiddleware.js"; // Protect admin routes

// const router = express.Router();

// router.post("/", verifyAdmin, createCampsite); // Admin Only
// router.get("/", getAllCampsites);
// router.get("/:id", getCampsiteById);
// router.put("/:id", verifyAdmin, updateCampsite); // Admin Only
// router.delete("/:id", verifyAdmin, deleteCampsite); // Admin Only

// export default router;
